To build PicoMail, type:
./autogen.sh (in the apps directory)
make

You now have the picomail binary.

Further install:

cp picomail.conf ~/.picomail.conf
chmod 600 ~/.picomail.conf

And edit ~/.picomail.conf

