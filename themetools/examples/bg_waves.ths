/* A spiffy little background made with the help of gimp and OpenGL */

obj default name = "Waves background";

object background {
   bitmap1 = LoadBitmap("data/tilable_waves.jpeg");
   bgfill = fillstyle {
      Bitmap(x,y,w,h,bitmap1);
   };
}

/* The End */
